apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: todo-server-monitor
  namespace: monitoring
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: server
  endpoints:
  - port: http-port
    path: /metrics